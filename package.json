{
  "name": "battlesnails",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "mp:server": "node server/index.mjs",
    "test": "node --test",
    "probe:controls": "node scripts/probe-controls.mjs"
  },
  "devDependencies": {
    "vite": "^5.0.8"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
} 
